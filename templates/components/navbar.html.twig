<nav class="site-navbar" aria-label="Navigation principale">
    <div class="site-navbar__inner">
        <a href="{{ path('app_home') }}" class="site-navbar__brand">
            <img src="/logo.png" alt="Logo {{ app_name }}" class="site-navbar__logo">
            <span class="site-navbar__title">{{ app_name }}</span>
        </a>
    </div>
</nav>
